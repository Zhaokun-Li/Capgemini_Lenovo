# 联想3C舆情模型网站接入说明

## 1. 当前Notebook模型结构

- 最终模型：XGBoost
- 标签：评分 <= 3 为差评（0），评分 >= 4 为好评（1）
- 文本特征：3000维 TF-IDF
- 结构化特征：
  - 产品系列独热编码
  - 内存编码
  - 硬盘编码
  - 重复购买情况编码
  - 评论长度
  - SnowNLP情感分数
- 模型额外识别：
  - 真实差评
  - 高分低评
  - 风险等级

## 2. 先导出模型

打开原Notebook，运行至XGBoost训练完成，然后复制运行：

`Notebook末尾追加代码.txt`

生成：

`Backend/analysis/xgb_sentiment_bundle.joblib`

注意：只保存xgb_model不够，必须一起保存TF-IDF和三个编码器，否则网站导入新数据时无法生成与训练阶段一致的特征。

## 3. 安装后端依赖

进入Backend目录：

```bash
pip install -r requirements_model.txt
```

## 4. 注册Flask蓝图

在原有 `app.py` 中增加：

```python
from routes.import_analysis import analysis_bp
app.register_blueprint(analysis_bp)
```

不要重复创建新的Flask实例。

## 5. 前端安装ECharts

```bash
npm install echarts axios
```

将：

- `Frontend/src/api/analysis.js`
- `Frontend/src/views/DataImportAnalysis.vue`

复制到你项目相应位置。

## 6. 添加Vue路由

```javascript
{
  path: "/data-import-analysis",
  name: "DataImportAnalysis",
  component: () => import("../views/data_import_analysis.vue"),
  meta: {
    title: "数据导入与分析",
    requiresAuth: true,
    adminOnly: true
  }
}
```

## 7. 导入文件必须包含

- 电脑配置
- 评论内容
- 有用数
- 重复购买情况
- 评分
- 内存
- 硬盘

可选字段：

- 评论日期 或 date
- 产品系列

## 8. 当前接口

### POST `/api/import-analysis`

上传Excel或CSV并立即返回全部分析。

FormData字段：

`file`

返回内容：

- overview：总评论数、平均评分、差评数、差评率、高风险数、高分低评数
- sentiment：好评/差评分布
- product：产品系列评论量、平均评分、差评率
- memory：内存配置差评率
- storage：硬盘配置差评率
- trend：每月评论数、差评数、平均评分、差评率
- keywords：问题评论Top20高频词
- risks：高风险评论列表

### GET `/api/analysis/latest`

获取最近一次分析结果。

## 9. 正式项目需要继续补充

当前示例将最近结果保存在Flask内存中，重启后会消失。正式项目应当：

1. 将清洗后和预测后的评论写入MySQL。
2. 建立 `analysis_tasks` 表记录每次导入。
3. 建立 `public_opinion` 表保存每条评论的模型标签、差评概率、风险等级和问题类型。
4. 图表接口从MySQL读取，而不是长期依赖内存。
5. 大文件应改为后台任务队列，避免HTTP请求超时。
